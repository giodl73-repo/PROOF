name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Clone mdpath sibling
        run: git clone https://github.com/giodl73-repo/MDPATH ../mdpath

      - name: Check out pinned mdcrop sibling
        run: |
          git init ../mdcrop
          git -C ../mdcrop fetch --depth 1 https://github.com/giodl73-repo/MDCROP.git 4c3f6e6a7388062575ea37b869fb159d1298fabc
          git -C ../mdcrop checkout --detach FETCH_HEAD

      - name: Build
        run: cargo build --all-targets

      - name: Test (unit + integration)
        run: cargo test

      - name: Prove real mdcrop compatibility
        run: cargo test --test integration_tests binary_real_mdcrop_

      - name: Test (release mode)
        run: cargo test --release

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - name: Clone mdpath sibling
        run: git clone https://github.com/giodl73-repo/MDPATH ../mdpath
      - run: cargo fmt --check
      - run: cargo clippy -- -D warnings

  e2e:
    name: E2E smoke test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Clone mdpath sibling
        run: git clone https://github.com/giodl73-repo/MDPATH ../mdpath
      - name: Build debug binary
        run: cargo build
      - name: E2E — clean file exits 0
        run: ./target/debug/proof check tests/fixtures/perfect_box.md
      - name: E2E — broken file exits 1
        run: |
          ./target/debug/proof check tests/fixtures/width_mismatch.md && exit 1 || true
      - name: E2E — rich format is valid JSON
        run: |
          ./target/debug/proof check --format rich --no-fail tests/fixtures/width_mismatch.md | python3 -c "import sys,json; json.load(sys.stdin)"
      - name: E2E — stats command
        run: ./target/debug/proof stats tests/fixtures/
      - name: E2E — layout command
        run: |
          echo '```' > /tmp/fig.md
          echo 'BOX CONTENT' >> /tmp/fig.md
          echo '└─────────┘' >> /tmp/fig.md
          echo '```' >> /tmp/fig.md
          ./target/debug/proof layout /tmp/fig.md /tmp/fig.md --gap 4 --labels "A" "B"

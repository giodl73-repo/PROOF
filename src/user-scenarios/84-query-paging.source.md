# US-84 — ?skip + ?top combine for SQL-style paging

Skip the first model (baseline), then take the next two. Result: GRU-v2 +
Transformer-S.

```proof:table
md://src/user-scenarios/data/models.md#:table:0?skip=1&top=2
```

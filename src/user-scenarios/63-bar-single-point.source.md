# US-63 — Bar chart with a single data point

Edge case: a chart with exactly one data point. The bar should render at
the full bar-area width (since max=value).

```proof:chart kind=bar width=40
Total: 100
```

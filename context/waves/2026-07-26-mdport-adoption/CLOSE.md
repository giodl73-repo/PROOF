# MDPORT adoption closeout

PROOF now emits `mdport.v1` through `proof compile --target mdport`.
Markdown compilation, directive resolution, diagnostics, and artifact manifests
remain PROOF-owned; MDPORT is only the portable transfer representation.
